// TODO: Business logic lives here — keep routes thin
// Ideas:
//   - generateShortCode() — nanoid or base62 of id
//   - createShortUrl(longUrl) — insert, handle duplicates
//   - findByCode(code) — lookup for redirect
//   - incrementClickCount(code) — optional extra
